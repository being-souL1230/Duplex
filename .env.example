# PostgreSQL connection ($ must stay URL-encoded as %24 if present)
DATABASE_URL="postgresql://postgres:yourpassword@127.0.0.1:5432/app_db"

# Google OAuth - https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: {origin}/api/auth/google/callback
# e.g. http://localhost:3000/api/auth/google/callback for dev
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
